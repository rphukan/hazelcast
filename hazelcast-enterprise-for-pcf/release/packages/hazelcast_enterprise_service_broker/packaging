# abort script on any command that exit with a non zero value
set -e

cp hazelcast_enterprise_service_broker/hazelcast-cf-service-broker.war ${BOSH_INSTALL_TARGET}
cp hazelcast_enterprise_service_broker/manifest.yml ${BOSH_INSTALL_TARGET}
